# `src/plot_confusion.py`

## Função

Gera matrizes de confusão / figuras de avaliação para runs específicos (cenários `base_F_calculated`, `base_D_calculated` etc. configurados no próprio script). Útil para slides e o manuscrito do TCC.

## CLI

`python src/plot_confusion.py`
