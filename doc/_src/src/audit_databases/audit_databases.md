# `src/audit_databases.py`

## Função

Auditoria das **bases de modelagem** (parquets em `data/modeling/`): estatísticas por cenário/ano, missing, distribuições úteis para decidir cortes e comparar versões `calculated` / `tsfusion`.

## CLI

`python src/audit_databases.py` — detalhes em `main()`.
